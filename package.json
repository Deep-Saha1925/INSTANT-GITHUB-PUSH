{
  "name": "instant-github-push",
  "displayName": "instant-github-push",
  "description": "",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.105.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:extension.createAndPushRepo"
  ],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "instant-github-push.helloWorld",
        "title": "Hello World"
      },
      {
        "command": "extension.createAndPushRepo",
        "title": "Create & Push to GitHub"
      }
    ]
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "npm run lint",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/vscode": "^1.105.0",
    "@vscode/test-cli": "^0.0.11",
    "@vscode/test-electron": "^2.5.2",
    "eslint": "^9.36.0"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "node-fetch": "^2.7.0",
    "open": "^10.2.0",
    "vscode": "^1.1.37"
  }
}
